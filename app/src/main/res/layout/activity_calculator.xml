<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root_activity_calculator"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="ir.pallas.calculator.CalculatorActivity">

    <fragment
        android:id="@+id/fragment_display"
        android:name="ir.pallas.calculator.DisplayFragment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        tools:layout="@layout/fragment_display" />

    <fragment
        android:id="@+id/fragment_input"
        android:name="ir.pallas.calculator.InputFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/fragment_display"
        tools:layout="@layout/fragment_input" />

</RelativeLayout>
